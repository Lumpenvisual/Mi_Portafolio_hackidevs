; eslint-plugin-jsx-a11y@6.10.2 caps its eslint peer at v9, but the project runs
; eslint 10. The tree resolves fine in practice; tell npm to accept it so fresh
; installs (e.g. Vercel's build) don't fail with ERESOLVE.
legacy-peer-deps=true
