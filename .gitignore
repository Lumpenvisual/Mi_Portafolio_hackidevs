# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
.moodboard/

# Phase 2 analysis: cloned reference repos (NOT part of the portfolio)
_repos/

# Raw source art — huge; optimized WebPs are committed under public/
assests/
hacki-perfil-real.png

# Raw 3D model — 44 MB; must be optimized (Draco/meshopt) before committing
public/models/vintage_slr_camera/

# allan-pinot reference build (NOT part of the portfolio)
allan-pinot/

# autoskills skill bundles + agent memory (skills-lock.json is the tracked source of truth)
.claude/skills/
.claude/agent-memory/
.agents/

# Playwright
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
